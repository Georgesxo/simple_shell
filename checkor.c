#include "shell.h"

/**
 * check_or- handle logical operator or
 * @arg1: parameter
 * @arg2: parameter
 * Return: a value
 */

bool check_or(int arg1, int arg2)
{
	int x, y;

	return (arg1 < x ||  arg2 > y);
}
